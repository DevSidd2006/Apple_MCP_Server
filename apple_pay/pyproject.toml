[project]
name = "apple_pay"
version = "0.1.0"
description = "Apple Pay MCP server for payment processing and wallet management"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.10.1",
]
